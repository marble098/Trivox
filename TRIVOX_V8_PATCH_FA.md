# بسته اصلاحی Trivox v8

مبنای بررسی: شاخه `main` و commit زیر:

`ae97c701d32b81e6d3b628175442c12dcc0625fc`

## نصب دستی

محتویات این ZIP را در ریشه ریپوی Trivox کپی کنید و اجازه دهید فایل‌های هم‌نام جایگزین شوند. ساختار داخل ZIP از ریشه پروژه شروع می‌شود و مسیرها آماده‌اند.

## اصلاحات اصلی

- وضعیت `Connected` فقط پس از عبور موفق ترافیک HTTPS از مسیر زنده اعلام می‌شود؛ در حالت VPN فقط خود مسیر Android VPN بررسی می‌شود و پروکسی جانبی نمی‌تواند خرابی VPN را پنهان کند.
- بررسی آغاز اتصال برای همه پروتکل‌ها اجرا می‌شود، نه فقط WireGuard.
- زمان‌بندی بررسی شروع برای مسیرهای موبایل و WireGuard با بودجه محدود، چند نمونه، تقسیم صحیح زمان و لغو سریع اصلاح شده است.
- DNS داخلی Xray دارای tag مسیریابی صریح است تا DNSهای خصوصی واردشده مانند `10.x.x.x` به اشتباه Direct نشوند.
- برای دامنه endpoint یک bootstrap DNS محدود ایجاد می‌شود تا DNS-via-proxy وارد حلقه نشود.
- نرمال‌سازی WireGuard شامل alias کلیدها، AllowedIPs، keepalive، Reserved، MTU محافظه‌کارانه، حذف streamSettings و رد واضح فیلدهای مخصوص AmneziaWG است.
- دریافت Exit IP دارای سه مسیر fallback و خواندن محدود پاسخ است.
- کارت‌های لیستی و گریدی و سه دکمه عملیات پایین صفحه با ظاهر تخت، بدون elevation و فشرده بازطراحی شده‌اند.
- عدد TCP قدیمی برای WireGuard نمایش داده نمی‌شود؛ تست واقعی Xray جایگزین آن می‌شود.

## اعتبارسنجی داخل بسته

- تمام XMLها با parser استاندارد XML بررسی شده‌اند.
- مسیرها، IDهای مورد استفاده در `ProfileAdapter` و فایل‌های تست کنترل شده‌اند.
- تست‌های واحد جدید برای وضعیت HTTP health check، DNS routing و نرمال‌سازی WireGuard افزوده شده‌اند.

برای تست نهایی داخل ریپو:

```bash
./gradlew --no-daemon testDebugUnitTest
./gradlew --no-daemon lintDebug
./gradlew --no-daemon assembleDebug
```
