# Core input

Put a manually downloaded official `libxray-android.zip` or `libXray.aar` here, then run the wizard with `--local-core` / `-LocalCore`.

Do not commit native core files. Standalone Xray command-line archives are inspected and rejected because they are not Android AAR/JNI integration artifacts.
