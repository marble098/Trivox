mixed-port: 7893
allow-lan: true
dns:
  enable: true
  listen: 0.0.0.0:53
proxy-groups:
  - name: Auto
    type: url-test
    use: [proxy]
proxy-providers:
  proxy:
    type: http
    url: >-
      https://example.com/provider.yaml
    interval: 3600
rules:
  - MATCH,Auto
